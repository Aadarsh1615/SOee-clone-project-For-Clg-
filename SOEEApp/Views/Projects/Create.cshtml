@model SOEEApp.Models.Project

@{
    ViewBag.Title = "Create Project";
}

<h2>Create Project</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-group">
        @Html.LabelFor(m => m.Name)
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.Label("OIC")
        @Html.DropDownListFor(model => model.OICUserId, (SelectList)ViewBag.OICUserId, "Select OIC", new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.ProjectCost)
        @Html.TextBoxFor(m => m.ProjectCost, new { @class = "form-control", type = "number", step = "0.01", min = "0" })
        @Html.ValidationMessageFor(m => m.ProjectCost, "", new { @class = "text-danger" })
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
}
